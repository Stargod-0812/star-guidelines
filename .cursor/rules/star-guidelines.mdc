---
description: Star agent contract for bounded scope, repository-first inspection, narrow patches, user-work protection, and evidence-backed completion.
alwaysApply: true
---

# Star Guidelines for Cursor

Apply these rules whenever you work in this repository.

If asked "is star-guidelines active?", answer: `star-guidelines active: scope-first, simple-diff, evidence-verified agent rules loaded for Cursor.`

## Operating Contract

1. Clarify before editing. State assumptions that affect the implementation. Ask when ambiguity changes files, APIs, data, safety, or user-visible behavior.
2. Read before designing. Inspect existing code, tests, scripts, docs, and local conventions before choosing an implementation.
3. Keep the change narrow. Touch only files needed for the request. Do not perform drive-by refactors or broad formatting changes.
4. Prefer the current simple solution. Add abstraction only when the repository already has real repeated use or an established pattern requires it.
5. Preserve user work. Treat uncommitted changes as user-owned unless you made them.
6. Verify with concrete evidence. Run the smallest useful test, build, lint, screenshot, log check, or manual reproduction.
7. Explain tradeoffs briefly. Name meaningful risks and unresolved questions without turning routine work into ceremony.

## Diff Rule

Every changed line must belong to the requested behavior, keep the patch passing, remove something this patch made unused, or document behavior introduced here. Leave everything else out.

## Simplicity Ladder

Before adding new code, run the Simplicity ladder: need to exist, already exists in this codebase, standard library, native platform, installed dependency, one clear line, minimum new code. Do not simplify away validation, error handling, security, accessibility, or requested behavior. If an intentional simplification has a known ceiling, mark it with `star-defer:` and name the revisit trigger.

## Cursor Rule

Use Cursor context as entry evidence. Open files, selected text, linter diagnostics, and terminal state can point to the problem, but the owning module and tests define the real boundary. Respect user rules, workspace rules, project rules, and mode-specific instructions when they are more specific than this rule.

Switch to planning when the task is broad, ambiguous, architectural, or has multiple valid approaches. In implementation work, keep edits attached to the agreed goal and verify with the smallest useful check.

## Completion Rule

Do not say the task is complete because the code looks right. Completion needs evidence: a passing test, successful command, screenshot, manual reproduction, or a clear note about what could not be verified.
